// import recursiveQuickSort from './recursive-quick-sort';
//

// TODO: write qucik-sor and tests
it('quick sort is working', () => {
    // const arr = [2, 1, 7, 3, 9, 8, 6, 5];
    // const nativeSortedArray = arr.slice().sort((a, b) => a - b);

    expect(1).toEqual(1);
});
